<UserControl x:Class="Naviguard.Views.AssignUserToGroups"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Naviguard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Primer Border en la primera fila con filtros -->
        <Border Grid.Row="0" Background="White" CornerRadius="30" Padding="20" Effect="{DynamicResource ShadowEffect}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Nombre -->
                <TextBlock Text="Nombre:" FontSize="14" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Margin="0,0,0,5"/>
                <TextBox Grid.Row="1" Grid.Column="0" Width="200" Height="30" Margin="0,0,10,0" Style="{StaticResource RoundedTextBoxStyle}"/>

                <!-- Área -->
                <TextBlock Text="Área:" FontSize="14" FontWeight="Bold" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Width="200" Height="30" Margin="0,0,10,0">
                    <ComboBoxItem Content="Selecciona área" IsSelected="True"/>
                </ComboBox>

                <!-- Categoría -->
                <TextBlock Text="Categoría:" FontSize="14" FontWeight="Bold" Grid.Row="0" Grid.Column="2" Margin="0,0,0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="2" Width="200" Height="30" Margin="0,0,10,0">
                    <ComboBoxItem Content="Selecciona categoría" IsSelected="True"/>
                </ComboBox>

                <!-- Botón Filtrar -->
                <Button Grid.Row="1" Grid.Column="3" Width="100" Height="30" Content="Filtrar"
                         Style="{StaticResource PrimaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Segunda fila con dos columnas -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Border en la primera columna -->
            <Border Grid.Column="0" Background="White" CornerRadius="30" Padding="20" Effect="{DynamicResource ShadowEffect}">
            </Border>

            <!-- Border en la segunda columna -->
            <Border Grid.Column="2" Background="White" CornerRadius="30" Padding="20" Effect="{DynamicResource ShadowEffect}">
            </Border>
        </Grid>
    </Grid>
</UserControl>
